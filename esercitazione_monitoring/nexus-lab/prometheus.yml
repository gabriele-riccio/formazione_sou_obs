global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'nexus'
    metrics_path: /service/rest/metrics/prometheus
    basic_auth:
      username: metrics
      password: __NEXUS_METRICS_PASSWORD__
    static_configs:
      - targets: ['nexus:8081']
